You are integrating the neutral `SelectProperty` primitive from `@tc96/properties` as a priority field in an existing React interface.

Define the priority scale, identifiers, labels, ordering, icons, and colors in the consumer. Keep the value controlled and persist changes outside the component. Do not make `@tc96/properties` responsible for business-specific priority levels, sorting rules, authorization, or mutations.
