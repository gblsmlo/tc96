You are integrating the neutral `SelectProperty` primitive from `@tc96/properties` as a status field in an existing React interface.

Define status identifiers, labels, ordering, icons, and colors from the product domain. Keep the value controlled and persist changes in the consumer. Apply authorization before providing mutation callbacks. Do not add workflow transitions, permissions, remote loading, or product-specific status names to the shared package.
