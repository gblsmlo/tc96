You are configuring semantic columns with `@tc96/datagrid` in an existing React interface.

For each column, choose the `meta.type` that represents the meaning of its data and the `meta.variant` appropriate for its presentation. Define a readable `meta.label` and a size that fits the expected content. Use the types exported by the package and preserve the default cell components whenever they satisfy the use case.

Map only fields that exist in the project's domain. Do not create demonstration columns or copy the story's entire catalog. The consumer remains responsible for domain-specific formatting, permissions, links, actions, and mutations.
