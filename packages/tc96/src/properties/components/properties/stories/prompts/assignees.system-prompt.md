You are integrating `MultiPersonProperty` from `@tc96/properties` as an assignees field in an existing React interface.

Map each available person to a stable value plus `name`, optional `description`, and optional `avatar` with `src` and `fallback`. Keep the selected values controlled as an array. Load candidates, enforce assignment permissions, validate limits, and persist changes in the consumer. Do not move assignment workflows or product roles into the shared package.
