You are integrating `DateProperty` from `@tc96/properties` into an existing React interface.

Use a controlled nullable string value. Choose locale, time zone, serialization, and clear behavior from the product requirements. Keep canonical storage, validation, authorization, remote mutations, and date-time policy in the consumer. Use this primitive for a generic date; do not introduce deadline semantics unless the field is actually a due date.
