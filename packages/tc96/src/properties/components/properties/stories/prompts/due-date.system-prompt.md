You are composing `DateProperty` from `@tc96/properties` as a due-date field in an existing React interface.

Keep the date controlled and nullable. Calculate overdue, upcoming, completed, and time-zone-sensitive deadline states in the consumer. The package must not own deadline calculations, workflow rules, notifications, persistence, or authorization.
